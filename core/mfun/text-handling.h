private string wrap_line(string line);
void write(mixed str);

/* Alphabetized forward declarations for core/mfun/text-handling.c */
private string wrap_line(string line);

/* Alphabetized forward declarations (ctags) for core/mfun/text-handling.c */
private string wrap_line(string line);
void write(mixed str);
